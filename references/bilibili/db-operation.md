## 六、程序操作数据库设置规范

### 6.1 必须遵守

#### B-085 长连接自动重连【必须】

【必须】如果应用使用的是长连接，应用必须具有自动重连的机制，但请避免每执行一个 SQL 去检查一次 DB 可用性。

#### B-086 长连接 TIMEOUT 检查【必须】

【必须】如果应用使用的是长连接，应用应该具有连接的 TIMEOUT 检查机制，及时回收长时间没有使用的连接，TIMEOUT 时间一般建议为 2 小时。

#### B-087 连接字符集【必须】

【必须】程序访问数据库连接的字符集请设置为 `utf8mb4`。

### 6.2 绝对禁止

#### B-088 禁止程序中 DDL【绝对禁止】

【绝对禁止】程序中禁止一切 DDL 操作。
